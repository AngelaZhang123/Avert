<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.extinguisher">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.SET_ALARM" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ChecklistSettingsActivity"
            android:theme="@style/CustomTheme"/>
        <activity
            android:name=".PopupActivity2"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />
        <activity
            android:name=".PopupActivity"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />
        <activity android:name=".EQLevel2Activity" />
        <activity android:name=".HouseActivity" />
        <activity android:name=".EQLevel1Activity" />
        <activity android:name=".SimulationActivity" />
        <activity android:name=".GameOverActivity" />
        <activity android:name=".CampfireActivity" />
        <activity android:name=".EarthquakeActivity" />
        <activity android:name=".WildfireActivity" />
        <activity android:name=".InfoActivity" />
        <activity android:name=".CheckListActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <receiver android:name=".NotificationReceiver"/>
    </application>

</manifest>